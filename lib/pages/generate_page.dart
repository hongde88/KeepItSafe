import 'package:flutter/material.dart';

class GeneratePage extends StatelessWidget {
  static const int pageIdx = 3;

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.redAccent,
    );
  }
}
